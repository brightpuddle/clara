#:schema https://json.schemastore.org/any.json

env_files = []
root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
  args_bin = []
  pre_cmd = ["templ generate -path ./internal/webui/templ"]
  cmd = "go install ./..."
  delay = 1000
  entrypoint = ["clara", "serve"]
  exclude_dir = ["assets", "tmp", "vendor", "testdata", "dist", "node_modules"]
  exclude_regex = ["_test.go", ".*_templ.go"]
  exclude_unchanged = false
  follow_symlink = false
  full_bin = ""
  ignore_dangerous_root_dir = false
  include_ext = ["go", "templ"]
  kill_delay = "0s"
  log = "build-errors.log"
  rerun = false
  rerun_delay = 500

[screen]
  clear_on_rebuild = false
  keep_scroll = true
